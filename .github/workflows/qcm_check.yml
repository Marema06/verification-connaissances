name: Vérification et envoi QCM

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout du code
        uses: actions/checkout@v3

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 📦 Installer les dépendances
        run: |
          npm install
          cd server
          npm install

      - name: 🤖 Générer et envoyer le QCM
        run: |
          node server/server.js
        env:
          HF_API_KEY: ${{ secrets.HF_API_KEY }}
          EMAIL_USER: ${{ secrets.EMAIL_USER }}
          EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}

      - name: 🚀 Déployer l'application Angular
        run: |
          cd client
          npm install
          ng build --prod

      - name: 📦 Déployer sur serveur (exemple: serveur distant ou hébergement)
        run: |
          scp -r client/dist/ /path/to/your/deploy
